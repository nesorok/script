using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Destroy : MonoBehaviour
{
    void Start()
    {
        // Команда Destroy(gameObject) уничтожает текущий игровой объект (на котором находится скрипт).
        Destroy(gameObject);
    }

    void Update()
    {

    }
}